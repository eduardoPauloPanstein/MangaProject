@model MvcPresentationLayer.Models.AnimeModel.AnimeItemModalViewModel

@{

    ViewData["Title"] = @Model.Anime.canonicalTitle;
    string Img = @Model.Anime.AnimeCoverImage;
    string width = "100%";
    int height = 400;
    if (Model.Anime.AnimeCoverImage == null)
    {
        //width = "25";
        //height = 400;
        Img = Model.Anime.AnimePosterImage;
    }
}


<div class="row" id="Banner">
    <!-- Background image -->
    <img class="" width="@width" height="@height" src="@Img" alt="Background image">
</div>
<!-- Page Content -->
<section class="py-5">
    <div class="container">
        <h2 class="fw-light">@Model.Anime.canonicalTitle</h2>
        <h5>Synopsis:</h5>
        <p>@Model.Anime.synopsis</p>
    </div>

    <div class="form-group">
        <div class="card-item">
            <a class="custom-link" href="/Anime/UserFavorite/@Model.Anime.Id">
                <input type="submit" value="Favoritar" class="btn btn-primary" />
            </a>
        </div>
    </div>
    @*<div class="form-group">
        <div class="card-item">
            <H1>
                Format
{@Model.Anime.subtype}
Episodes
{@Model.Anime.episodeCount}

Episode Duration
{@Model.Anime.episodeLength}

Status
{@Model.Anime.status.ToString()}

Start Date
{@Model.Anime.startDate}

End Date
{@Model.Anime.endDate}

Average Score
{@Model.Anime.averageRating}

Popularity
545537

Favorites
{@Model.Anime.favoritesCount}

Genres
@{foreach (var item in @Model.Anime.Categories)
                {
                    <h1>@item.Name</H1>
                }
}
            </H1>
        </div>
    </div>*@
</section>




